PORT=4000

# PostgreSQL (run via Docker: see SETUP.md Step 2)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lecture_app
DB_USER=karthik
DB_PASSWORD=secret123

# Redis (run via Docker: see SETUP.md Step 2)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=supersecret12345
JWT_EXPIRES_IN=24h
